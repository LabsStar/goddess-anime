# 559


The error code `559` means that you have entered an invalid url. This could be because the url does not exist or you do not have permission to view the url.

## Enum Name

```ts
enum ErrorCodes {
    NO_PAGE = 559,
}

export default ErrorCodes;
```