# 356


The error code `356` means that you are currently logged in and you are trying to access the login page. This could be because you are already logged in or because you do not have permission to access it. 


## Enum Name

```ts
enum ErrorCodes {
    LOGGED_IN = 356,
}

export default ErrorCodes;
```