# 465


The error code `465` means that the content you are trying to access is not available. This could be because the content was deleted or because you do not have permission to access it.


## Enum Name

```ts
enum ErrorCodes {
    NO_CONTENT = 465,
}

export default ErrorCodes;
```