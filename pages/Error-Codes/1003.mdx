# 1003


The error code `1003` means Default error message. This could be that the error message is not set.

## Enum Name

````ts
enum ErrorCodes {
    DEFAULT_ERROR = 1003,
}

export default ErrorCodes;
````