# 680


The error code `680` means that the user you are trying to view does not exist or has been deleted from discord's database.


## Enum Name

```ts
enum ErrorCodes {
    USER_NOT_FOUND = 680,
}

export default ErrorCodes;
```