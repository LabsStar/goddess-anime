# 678

The error code `678` means that you have entered an Invalid Shop ID. This could be because the Shop ID has been deleted or the Shop ID is not valid [BSON](https://bsonspec.org/)

## Enum Name

```ts
enum ErrorCodes {
    SHOP_NOT_FOUND = 678,
}

export default ErrorCodes;
```