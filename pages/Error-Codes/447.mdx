# 447


The error code `447` means that you have encountered an Unknown error. This could be because the error message is not set or the error message is not valid.

## Enum Name

```ts
enum ErrorCodes {
    UNKOWN_ERROR = 447,
}

export default ErrorCodes;
```