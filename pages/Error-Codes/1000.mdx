# 1000


The error code `1000` means that you have entered an Invalid Shop ID. This could be because the Shop ID does not exist or the Shop ID is not valid [BSON](https://bsonspec.org/)

## Enum Name

```ts
enum ErrorCodes {
    INVALID_SHOP_ID = 1000,
}

export default ErrorCodes;
```