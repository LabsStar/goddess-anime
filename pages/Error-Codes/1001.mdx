# 1001


The error code `1001` means that you have entered an Invalid Card ID. This could be because the Card ID does not exist or the Card ID is not valid [BSON](https://bsonspec.org/)

## Enum Name

```ts
enum ErrorCodes {
    INVALID_CARD_ID = 1001,
}

export default ErrorCodes;
```
